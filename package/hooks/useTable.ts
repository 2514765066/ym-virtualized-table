import { ref } from "vue";

//容器宽度
export const containerWidth = ref(0);

//容器高度
export const containerHeight = ref(0);

//行开始索引
export const rowStartIndex = ref(0);

//列开始的索引
export const colStartIndex = ref(0);
